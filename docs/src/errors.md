```@meta
CurrentModule = ClusterAlgebras
```

# Errors

The package throws typed errors; every one is a subtype of ClusterAlgebraError.

```@autodocs
Modules = [ClusterAlgebras]
Private = false
Pages = ["src/errors.jl"]
```
