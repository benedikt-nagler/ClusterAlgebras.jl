```@meta
CurrentModule = ClusterAlgebras
```

# Quivers

Construction, mutation and isomorphism testing of quivers, plus the named families and random generation.

```@autodocs
Modules = [ClusterAlgebras]
Private = false
Pages = ["src/quiver.jl", "src/named_quivers.jl", "src/canonical_form.jl", "src/random_quiver.jl", "src/show.jl"]
```
