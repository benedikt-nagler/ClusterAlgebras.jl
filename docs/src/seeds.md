```@meta
CurrentModule = ClusterAlgebras
```

# Seeds and mutation

Seeds carry a quiver together with its cluster. Mutation returns a new seed; the initial cluster is never modified.

```@autodocs
Modules = [ClusterAlgebras]
Private = false
Pages = ["src/seed.jl", "src/mutation.jl", "src/denominator_vector.jl"]
```
